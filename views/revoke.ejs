<!DOCTYPE HTML>
<html>
  <head>
    <title>Revoke <%= bot.users.get(bot.submission.get(id, "cli_id")).username %>  - DBR</title>
    <% include ./partials/portalHeader.ejs %>
  </head>
  <body>
    <% include ./partials/nav.ejs %>
    <div class="container">
      <h1>
       Revoke <%= bot.users.get(bot.submission.get(id, "cli_id")).username %> 
      </h1>
      <hr><br>
      <form action="/modportal/revoke" method="POST" enctype="application/x-www-form-urlencoded">
        <center>Are you sure you want to revoke approval for <%= bot.users.get(bot.submission.get(id, "cli_id")).username %>?<div style="color:red;display:inline-block;margin-bottom:0.5rem;"></div><br></center>
      <br><br>
        <input type="hidden" name="cli_id" value="<%= bot.submission.get(id, "cli_id") %>">
      <input type="hidden" name="revoker" value="<%= bot.submission.get(id, "submitter") %>">
      <center><input type="submit" class="btn btn-warning" title="Revoke" value="Revoke"></input>&nbsp;&nbsp;<a href="javascript:window.history.back()" class="btn btn-warning" title="Back">Cancel</a></center>
      </form><br><br>
  </body>
</html>